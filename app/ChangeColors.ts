 import {ctx, canvas, bgctx} from "./canvas"

window.addEventListener(
  "keydown",
  function(event){
    if (event.key=='1'){
      bgctx.strokeStyle = "Black"
      bgctx.fillStyle = "Black"
    }
  }
)

window.addEventListener(
  "keydown",
  function(event){;
    if (event.key=='2'){;
      bgctx.strokeStyle = "DarkRed";
      bgctx.fillStyle = "DarkRed";
    };
  }
);
window.addEventListener(
  "keydown",
  function(event){;
    if (event.key=='3'){;
      bgctx.strokeStyle = "Blue";
      bgctx.fillStyle = "Blue";
    };
  }
);
window.addEventListener(
  "keydown",
  function(event){;
    if (event.key=='4'){;
      bgctx.strokeStyle = "Purple";
      bgctx.fillStyle = "Purple";
    };
  });
window.addEventListener(
  "keydown",
  function(event){;
    if (event.key=='5'){;
      bgctx.strokeStyle = "Green";
      bgctx.fillStyle = "Green";
    };
  });
window.addEventListener(
  "keydown",
  function(event){;
    if (event.key=='6'){;
      bgctx.strokeStyle = "Yellow";
      bgctx.fillStyle = "Yellow";
    };
  });

  window.addEventListener(
  "keydown",
  function(event){;
    if (event.key=='7'){;
      bgctx.strokeStyle = "#3A1B06";
      bgctx.fillStyle = "#3A1B06";
    };
  });

  window.addEventListener(
  "keydown",
  function(event){;
    if (event.key=='8'){;
      bgctx.strokeStyle = "#666666";
      bgctx.fillStyle = "#666666";
    };
  });

    window.addEventListener(
  "keydown",
  function(event){;
    if (event.key=='9'){;
      bgctx.strokeStyle = "Orange";
      bgctx.fillStyle = "Orange";
    };
  });

window.addEventListener(
  "keydown",
  function(event){;
    if (event.key=='0'){;
      bgctx.strokeStyle = "#4a412a";
      bgctx.fillStyle = "#4a412a";
    };
  });

window.addEventListener(
  "keydown",
  function(event){;
    if (event.key=='e'){;
      bgctx.strokeStyle = "White";
      bgctx.fillStyle = "White";
    };
  });